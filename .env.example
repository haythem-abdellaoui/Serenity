# Local-only environment variables (copy to `.env`)
#
# `.env` is ignored by git (see `.gitignore`).
# Put secrets/tokens here for local development only.

# SonarQube token used by Maven Sonar plugin
SONAR_TOKEN=

