Place trained artifacts here (produced by training/train_model.py):

  crisis_model.pkl
  label_encoder.pkl
  feature_columns.pkl
  text_vectorizer.pkl
  text_risk_model.pkl
  medium_subtype_model.pkl (optional)
  medium_subtype_labels.pkl (optional)

If these files are missing, main.py still runs using a heuristic fallback.
